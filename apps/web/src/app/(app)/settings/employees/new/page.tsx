'use client';

import { EmployeeCard } from '../_components/employee-card';

/** moysklad «+ Сотрудник» — the same card in create mode. */
export default function EmployeeNewPage() {
  return <EmployeeCard />;
}
