{
  "//": "MASTER-TODO #29 — known-red source-scan guards. `scripts/check-guards.mjs` blocks a push that makes a NEW guard file fail, and tells you to shrink this list when a listed one starts passing. Add an entry ONLY with a todo ref + a reason; it is a debt register, not a mute button.",
  "generatedAt": "2026-07-28",
  "knownRed": [
    {
      "file": "src/__tests__/label-grounding.test.ts",
      "failing": 25,
      "todo": "#18 / #35",
      "reason": "docs/moysklad-reference/visual-captures is EMPTY in this checkout (git ls-files = 0). The capture corpus never came across with the adoption, so every capture-grounded label assertion has nothing to read. Unblocking needs a moysklad.uz login to re-capture — it is not fixable from source."
    }
  ],
  "//retired": "src/__tests__/raw-element-conventions.test.ts (#12) — cleared 2026-07-28: all 31 raw sites migrated to the DS primitives (textarea 3, select 5, input text/number 6, input date 5, checkbox 7, radio 11); the 3 remaining product-form-left-cards radios went into the test's own EXEMPT_RADIO with the documented interleaved-control rationale."
}
